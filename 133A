#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    string s;
    cin>>s;
    int check=0;
    string :: iterator it;
    for(it=s.begin();it!=s.end();it++)
    {
        if(*it=='H' || *it=='Q' || *it=='9' )
        {
            cout<<"YES";
            check=1;
             break;
        }
        
    }
    if(check==0)
    cout<<"NO";
}
